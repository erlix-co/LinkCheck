{
  "name": "linkcheck-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Dedicated Android app wrapper for LinkCheck",
  "type": "module",
  "scripts": {
    "build:web": "node ./scripts/build-web-mobile.mjs",
    "sync": "npx cap sync android",
    "build:android": "npm run build:web && npm run sync",
    "open:android": "npx cap open android",
    "doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.3.1",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/keyboard": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "typescript": "^6.0.3"
  }
}
